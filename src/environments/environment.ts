// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebaseConfig : {
    apiKey: "AIzaSyAb1I9bIar-Fk2FNcrFgaWiHgDC_OkVu_g",
    authDomain: "loginionic-5e0a2.firebaseapp.com",
    databaseURL: "https://loginionic-5e0a2.firebaseio.com",
    projectId: "loginionic-5e0a2",
    storageBucket: "loginionic-5e0a2.appspot.com",
    messagingSenderId: "693569485192",
    appId: "1:693569485192:web:ab2e7c5c657b157949e23d"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
